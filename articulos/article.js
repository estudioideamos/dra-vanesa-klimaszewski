document.addEventListener('DOMContentLoaded',()=>{const bar=document.querySelector('.reading-progress');const update=()=>{const h=document.documentElement.scrollHeight-innerHeight;if(bar)bar.style.width=(h>0?scrollY/h*100:0)+'%'};addEventListener('scroll',update,{passive:true});update();const reveal=el=>el.hasAttribute('data-reveal')?el.classList.add('is-visible'):el.classList.add('visible');const elements=document.querySelectorAll('.reveal,[data-reveal]');if('IntersectionObserver'in window){const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){reveal(entry.target);io.unobserve(entry.target)}}),{threshold:.08});elements.forEach(el=>io.observe(el))}else{elements.forEach(reveal)}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();if(window.lucide)lucide.createIcons()});